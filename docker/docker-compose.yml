version: '3.8'
services:
  certmagi:
    build:
      context: .
      dockerfile: docker/Dockerfile
    volumes:
      - ./certs:/home/certmagi/certs
      - ./scripts:/home/certmagi/scripts
    ports:
      - "8080:8080"
    environment:
      - IPFS_PATH=/home/certmagi/.ipfs
    command: bash
